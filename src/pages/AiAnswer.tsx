import React from 'react';
import styled from 'styled-components';

const AiAnswer = () => {
  return <AiAnswerContainer>AiAnswer</AiAnswerContainer>;
};

const AiAnswerContainer = styled.div``;

export default AiAnswer;
